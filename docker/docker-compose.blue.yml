#blue
version: '3'
services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    depends_on:
      - redis

  backend:
    build: .
    ports:
      - "8081:80"
    container_name: yello-blue
    environment:
      - SPRING_PROFILES_ACTIVE=blue
